import { useState } from 'react'
import { Alert, Button, Descriptions, Drawer, Form, Input, Modal, Progress, Select, Space, Table, Tag } from 'antd'
import { useQuery } from '@tanstack/react-query'
import { useNavigate } from 'react-router-dom'
import { useTranslation } from 'react-i18next'
import { ArrowUpRight, ClipboardCheck, Cpu, Database, Download, FileBarChart2, Fingerprint, HardDrive, Link2, LockKeyhole, Send, ShieldCheck } from 'lucide-react'
import { get, post } from './api/client'
import type { Audit, Export, Incident, Status, TeamMember, User } from './types'
import { Blank, Failure, Loading, SectionTitle, StateTag, time, useAction } from './ui'

function QueueMetrics({ groups }: { groups: { label: string; count: number }[] }) { return <div className="role-metrics">{groups.map(group => <section className="panel role-metric" key={group.label}><span>{group.label}</span><strong>{group.count}</strong></section>)}</div> }

export function ExpertPage({ user }: { user: User }) {
  const { t } = useTranslation()
  const navigate = useNavigate()
  const [selected, setSelected] = useState<Incident | null>(null)
  const [form] = Form.useForm()
  const { act, busy } = useAction()
  const query = useQuery({ queryKey: ['incidents', user.id], queryFn: ({ signal }) => get<{ items: Incident[] }>('/incidents', signal) })
  const transitions: Record<string, string[]> = { under_review: ['awaiting_explanation', 'confirmed', 'not_confirmed', 'insufficient_information'], awaiting_explanation: ['confirmed', 'not_confirmed', 'insufficient_information'], confirmed: ['corrective_actions', 'closed'], not_confirmed: ['closed'], insufficient_information: ['awaiting_explanation', 'confirmed', 'not_confirmed'], corrective_actions: ['closed'], closed: [] }
  return <><div className="page-heading"><div><span className="eyebrow">QUALITY / INDEPENDENT REVIEW</span><h1>{t('expert')}</h1><p>{t('expertSubtitle')}</p></div><div className="heading-icon"><ClipboardCheck size={25}/></div></div>{query.data && <QueueMetrics groups={[{ label: t('under_review'), count: query.data.items.filter(i => i.status === 'under_review').length }, { label: t('awaiting_explanation'), count: query.data.items.filter(i => ['awaiting_explanation', 'insufficient_information'].includes(i.status)).length }, { label: t('confirmed'), count: query.data.items.filter(i => ['confirmed', 'corrective_actions'].includes(i.status)).length }, { label: t('closed'), count: query.data.items.filter(i => ['closed', 'not_confirmed'].includes(i.status)).length }]}/>}<section className="panel"><SectionTitle title={t('expert')} subtitle={t('clinicalNote')}/>{query.isPending ? <Loading/> : query.error ? <Failure error={query.error}/> : query.data?.items.length ? <Table<Incident> rowKey="id" dataSource={query.data.items} pagination={{ pageSize: 8 }} scroll={{ x: 600 }} columns={[{ title: t('patient'), render: (_, record) => <button className="source-link" onClick={() => navigate(`/cases/${record.case_id}`)}>{record.case_alias}<ArrowUpRight size={14}/></button> }, { title: t('reason'), dataIndex: 'reason' }, { title: t('status'), render: (_, r) => <StateTag status={r.status}/> }, { title: t('time'), render: (_, r) => time(r.created_at) }, { title: '', render: (_, r) => <Button size="small" onClick={() => { form.resetFields(); setSelected(r) }}>{t('observe')}</Button> }]}/> : <Blank title={t('noData')} hint={t('expertSubtitle')}/>}</section><Drawer open={!!selected} title={t('expert')} onClose={() => setSelected(null)} width={560}>{selected && <><div className="review-case"><ClipboardCheck size={30}/><h2>{selected.case_alias}</h2><StateTag status={selected.status}/></div><p>{selected.reason}</p>{selected.decisions.map((d, i) => <article className="note-card" key={i}><StateTag status={d.status}/><p>{d.explanation}</p><small>{time(d.created_at)}</small></article>)}{user.role === 'expert' && selected.status !== 'closed' && <Form form={form} layout="vertical" className="margin-top" onFinish={values => void act(async () => { await post(`/incidents/${selected.id}/decisions`, { ...values, expected_version: selected.version }); setSelected(null) })}><Form.Item name="status" label={t('status')} rules={[{ required: true }]}><Select options={transitions[selected.status]?.map(v => ({ value: v, label: t(v) }))}/></Form.Item><Form.Item name="explanation" label={t('explanation')} rules={[{ required: true, min: 5 }]}><Input.TextArea rows={5}/></Form.Item><Button type="primary" htmlType="submit" loading={busy} block>{t('saveDecision')}</Button></Form>}</>}</Drawer></>
}

export function ReportsPage({ user }: { user: User }) {
  const { t } = useTranslation()
  const [create, setCreate] = useState(false)
  const [selected, setSelected] = useState<Export | null>(null)
  const [form] = Form.useForm()
  const { act, busy } = useAction()
  const reports = useQuery({ queryKey: ['exports', user.id], queryFn: ({ signal }) => get<{ items: Export[] }>('/exports', signal) })
  const incidents = useQuery({ queryKey: ['incidents', user.id], queryFn: ({ signal }) => get<{ items: Incident[] }>('/incidents', signal), enabled: user.role !== 'analyst' })
  const eligible = incidents.data?.items.filter(i => ['confirmed', 'corrective_actions', 'closed'].includes(i.status) && i.decisions.some(d => d.status === 'confirmed')) || []
  return <><div className="page-heading"><div><span className="eyebrow">QUALITY / REPORTING</span><h1>{t('reports')}</h1><p>{t('reportsSubtitle')}</p></div>{user.role !== 'analyst' && <Button type="primary" icon={<FileBarChart2 size={17}/>} onClick={() => setCreate(true)}>{t('createReport')}</Button>}</div><Alert type="info" showIcon message={t('reportNotice')} className="margin-bottom"/>{reports.data && <QueueMetrics groups={(user.role === 'analyst' ? ['sent'] : ['draft', 'approved', 'sent']).map(status => ({ label: t(status), count: reports.data.items.filter(r => r.status === status).length }))}/>}<section className="panel"><SectionTitle title={t('reports')} extra={<LockKeyhole size={19}/>}/>{reports.isPending ? <Loading/> : reports.error ? <Failure error={reports.error}/> : reports.data?.items.length ? <Table<Export> rowKey="id" dataSource={reports.data.items} pagination={false} scroll={{ x: 640 }} columns={[{ title: t('purpose'), render: (_, r) => <><strong>{r.purpose || `${t('aggregateReport')} · ${r.package.period}`}</strong><small className="block muted">{r.id.slice(0, 8)}</small></> }, { title: t('status'), render: (_, r) => <StateTag status={r.status}/> }, { title: t('time'), render: (_, r) => time(r.created_at) }, { title: '', render: (_, r) => <Button size="small" onClick={() => void act(async () => setSelected(await get<Export>(`/exports/${r.id}/preview`)), false)}>{t('preview')}</Button> }]}/> : <Blank title={t('noData')} hint={t('reportsSubtitle')}/>}</section><Modal open={create} title={t('createReport')} onCancel={() => setCreate(false)} footer={null}><Form form={form} layout="vertical" onFinish={values => void act(async () => { const result = await post<Export>('/exports', { ...values, recipient: 'mock-ministry' }); setCreate(false); setSelected(result); form.resetFields() })}><Form.Item name="incident_ids" label={t('expert')} rules={[{ required: true }]}><Select mode="multiple" options={eligible.map(i => ({ value: i.id, label: `${i.case_alias} · v${i.version}` }))} notFoundContent={t('noData')}/></Form.Item><Form.Item name="purpose" label={t('purpose')} rules={[{ required: true, min: 3 }]}><Input/></Form.Item><Form.Item name="basis" label={t('basis')} rules={[{ required: true, min: 3 }]}><Input.TextArea rows={3}/></Form.Item><Button type="primary" htmlType="submit" loading={busy} block>{t('createReport')}</Button></Form></Modal><Drawer open={!!selected} title={t('preview')} width={620} onClose={() => setSelected(null)}>{selected && <><div className="report-preview"><LogoReport/><span className="report-watermark">DEMONSTRATION</span><h2>{t('reports')}</h2><div className="report-fields">{Object.entries(selected.package).map(([key, value]) => <div key={key}><span>{key}</span><strong>{value === null ? '—' : String(value)}</strong></div>)}</div><div className="report-hash"><Fingerprint size={16}/><code>{selected.sha256}</code></div></div><Space wrap className="margin-top"><Button icon={<Download size={15}/>} href={`/api/v1/exports/${selected.id}/download?format=pdf`} target="_blank">PDF</Button><Button icon={<Download size={15}/>} href={`/api/v1/exports/${selected.id}/download?format=json`} target="_blank">JSON</Button>{user.role === 'sender' && selected.status === 'draft' && <Button type="primary" icon={<ShieldCheck size={15}/>} loading={busy} onClick={() => void act(async () => setSelected(await post<Export>(`/exports/${selected.id}/approve`, { expected_version: selected.version })))}>{t('approve')}</Button>}{user.role === 'sender' && selected.status === 'approved' && <Button type="primary" icon={<Send size={15}/>} loading={busy} onClick={() => void act(async () => setSelected(await post<Export>(`/exports/${selected.id}/send`, { expected_version: selected.version })))}>{t('send')}</Button>}</Space>{selected.receipt && <Alert className="margin-top" type="success" showIcon message={`${t('sent')} · ${selected.receipt.id}`} description={time(selected.receipt.received_at)}/>}</>}</Drawer></>
}
function LogoReport() { return <div className="report-logo"><img src="/mark.svg" alt=""/>AniqTashxis.ai</div> }

export function SettingsPage({ user }: { user: User }) {
  const { t } = useTranslation()
  const status = useQuery({ queryKey: ['status'], queryFn: ({ signal }) => get<Status>('/system/status', signal), refetchInterval: 10000 })
  const team = useQuery({ queryKey: ['team', user.id], queryFn: ({ signal }) => get<{ items: TeamMember[] }>('/team', signal), enabled: user.role === 'admin' })
  const audits = useQuery({ queryKey: ['audit', user.id], queryFn: ({ signal }) => get<{ items: Audit[] }>('/audit-events', signal), enabled: ['admin', 'quality', 'expert'].includes(user.role) })
  const rules = useQuery({ queryKey: ['rules'], queryFn: ({ signal }) => get<{ items: { id: string; name: string; version: string; approval_status: string }[] }>('/rules', signal) })
  if (status.isPending) return <Loading/>
  if (status.error || !status.data) return <Failure error={status.error}/>
  const data = status.data
  return <><div className="page-heading"><div><span className="eyebrow">INFRASTRUCTURE / TRANSPARENCY</span><h1>{t('settings')}</h1><p>{t('localHint')}</p></div><div className="heading-icon"><Cpu size={25}/></div></div><div className="system-grid">{[{ icon: Database, title: 'Database', value: data.database }, { icon: LayersIcon, title: 'Queue', value: data.queue }, { icon: Link2, title: 'DMED', value: data.dmed }, { icon: Send, title: t('reports'), value: data.export_transport }].map(({ icon: Icon, title, value }) => <section className="panel service-card" key={title}><Icon size={22}/><h3>{title}</h3><span className="service-value">{value}</span><span className="service-dot"/></section>)}</div><section className="panel margin-top model-card"><div className="model-symbol"><Cpu size={34} strokeWidth={1.4}/></div><div className="model-info"><span className="eyebrow">LOCAL INFERENCE / 4B PARAMETERS</span><h2>MedGemma 1.5</h2><p>{data.model.model_id}</p><Space wrap><Tag>{data.model.quantization}</Tag>{data.model.backend && <Tag>{data.model.backend}</Tag>}<Tag>{data.model.clinical_validation}</Tag><StateTag status={data.model.ready ? 'succeeded' : 'unavailable'}/></Space>{data.model.download_percent != null && <div className="margin-top"><p>{t('modelDownloading')}</p><Progress percent={data.model.download_percent} status="active"/></div>}{!data.model.ready && <Alert className="margin-top" type="warning" message={t(data.model.reason || 'modelMissing')} description={t('modelSetup')} showIcon/>}</div></section><section className="panel margin-top"><SectionTitle title={t('permission')} extra={<ShieldCheck size={20}/>}/><Descriptions column={{ xs: 1, sm: 2 }} items={[{ key: 'name', label: t('staffName'), children: user.name }, { key: 'role', label: t('permission'), children: t(user.role === 'expert' ? 'expertRole' : user.role) }, { key: 'org', label: 'Organization', children: user.tenant_id }, { key: 'mail', label: t('email'), children: user.email }]}/></section><section className="panel margin-top"><SectionTitle title={t('rule')} subtitle={t('noClinicalRules')}/><Table rowKey="id" pagination={false} dataSource={rules.data?.items} scroll={{ x: 600 }} columns={[{ title: t('rule'), dataIndex: 'id' }, { title: t('context'), dataIndex: 'name' }, { title: t('version'), dataIndex: 'version' }, { title: t('status'), render: () => <Tag color="orange">{t('demoRule')}</Tag> }]}/></section>{user.role === 'admin' && <section className="panel margin-top"><SectionTitle title={t('teamDirectory')} subtitle={t('teamHint')}/>{team.isPending ? <Loading/> : team.error ? <Failure error={team.error}/> : <Table<TeamMember> rowKey="id" dataSource={team.data?.items} pagination={false} scroll={{ x: 600 }} columns={[{ title: t('staffName'), dataIndex: 'name' }, { title: t('permission'), render: (_, member) => t(member.role === 'expert' ? 'expertRole' : member.role) }, { title: t('email'), dataIndex: 'email' }, { title: t('status'), render: (_, member) => <Tag color={member.active ? 'green' : 'default'}>{t(member.active ? 'active' : 'cancelled')}</Tag> }]}/>}</section>}{audits.data && <section className="panel margin-top"><SectionTitle title={t('audit')}/><Table<Audit> rowKey="id" dataSource={audits.data.items} pagination={{ pageSize: 8 }} scroll={{ x: 500 }} columns={[{ title: t('action'), dataIndex: 'action' }, { title: t('staffName'), dataIndex: 'actor_name' }, { title: t('resource'), render: (_, a) => <code>{a.resource_id.slice(0, 10)}</code> }, { title: t('time'), render: (_, a) => time(a.created_at) }]}/></section>}</>
}
function LayersIcon({ size }: { size: number }) { return <HardDrive size={size}/> }
